<%= form_tag(element_preuse_inspections_path(@element), method: :get) do %>
  <%= hidden_field_tag :element_id, @element.id %>
  <%= date_field_tag :date %>
  <%# There's JS I can impliment here to make this auto-go, right? %>
  <%# also, this directs weirdly, I think %>
  <%= submit_tag "View Inspection" %>
<% end %>

<%= show_inspection_if_selected %>
